# .env.example

# MongoDB connection string (replace with your actual credentials)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority&appName=<yourAppName>

# JWT secret key
JWT_SECRET=your_super_secret_key

# Application port
PORT=5000
